# dsh-academic-research 开发规则

本文件约束**代码怎么写**。**压缩后保留什么**由 [`docs/SUMMARY-SCHEMA.md`](docs/SUMMARY-SCHEMA.md)
规定；两者互不替代，也不要为了改善代码风格往插件里加 Schema 没有定义的功能。

# 科研代码执行原则

目标：按已确认方案完成最小充分实现，让研究者容易阅读、
检查和修改。正确性优先，不以行数最少为目标。

## 范围
只解决当前任务。先读取相关实现、规格和真实调用者，再修改。
沿用现有接口与依赖，不顺手重构无关代码。
不为未来需求新增配置项、兼容分支、框架或通用抽象。

## 实现
优先直白函数、明确变量和线性数据流。
抽取函数应当减少重复或隔离清晰职责，不为包装而包装。
不使用晦涩的一行式写法；保留有用的类型标注。
同一任务集合、顺序、指标定义和配置只维护一份权威定义。
注释解释科研口径、边界条件和不明显的理由，不复述代码。

## 正确性边界
不得擅改目标函数、任务范围、数据划分、baseline、
权重、分母、阈值平局规则、seed 角色和产物含义。
只在真实输入、外部调用和关键状态边界做必要检查，
不在内部逐层重复兜底。
不得用默认值、静默跳过、吞异常或未经规定的回退掩盖错误。
真实存在的并发、数据完整性和产物一致性要求不能省略。

## 歧义
普通工程细节自行处理。
影响实验含义或算法正确性的歧义，提交规格、相关代码、
实际调用者和最小反例，交给用户或决策模型。
只暂停受影响部分，不猜测补齐，也不为未知需求造框架。

## 验证
运行与本次改动直接相关的现有检查。
必要时补正常样例、关键反例或真实下游入口检查；
不追求测试数量与覆盖率，不默认新增测试基础设施。
预期结果来自规格或独立依据，不能照抄当前实现作为答案。
不通过削弱检查或修改预期来宣布成功。

## 完成
检查本次 diff，删除无用分支、重复状态、无意义包装和日志。
不改动或回滚用户已有的无关修改。
未经授权不提交、不覆盖科研产物、不启动昂贵正式实验。
完成后只报告：改了什么、运行了什么验证、还有什么未确认。

## 版本
一个版本 = 一组新功能测试稳定、可以上线。**不要每次测试修改就当一个版本**；
中间的修改留在工作区，直到该组功能稳定后再一次性成为下一个版本。
文档里不写逐次修订记录。
